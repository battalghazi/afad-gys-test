/**
 * Next.js Konfigürasyonu
 * 
 * Bu dosya Next.js uygulamasının davranışını yapılandırır.
 * Build optimizasyonları, routing kuralları, middleware ayarları burada tanımlanır.
 * 
 * Şu an minimal bir konfigürasyon kullanıyor - tüm ayarlar varsayılan değerlerde.
 * İhtiyaç duyulduğunda aşağıdaki özellikler eklenebilir:
 * - Image optimization ayarları
 * - Custom webpack konfigürasyonu  
 * - Environment variables
 * - Redirects ve rewrites
 * - Internationalization (i18n)
 * - Experimental features
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Minimal konfigürasyon - varsayılan ayarları kullan
  // Gelecekte gerekirse buraya ek ayarlar eklenebilir
};

export default nextConfig;
